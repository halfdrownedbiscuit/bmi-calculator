import { ReactComponent as Male } from 'raw/male.svg';
import { ReactComponent as Female } from 'raw/female.svg';
import { ReactElement } from 'react';

const iconTypes: {
  [key: string]: any;
} = {
  male: Male,
  female: Female
};

export default iconTypes;
